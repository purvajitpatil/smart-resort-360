[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# Depends(...) in argument defaults is the canonical FastAPI DI pattern.
ignore = ["B008"]

[tool.black]
line-length = 110